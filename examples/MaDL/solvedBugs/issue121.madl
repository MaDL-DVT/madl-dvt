// issue 121
// parentheses in bus expressions

const tok;

bus<2> mybus; // declare a bus

let mybus[2*(1-1)] := Source(tok); // assign bus[0]

let mybus[1] := Source(tok); // assign bus[1]

Sink(mybus[0]);
Sink(mybus[1]);